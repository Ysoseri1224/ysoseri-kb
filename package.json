{
  "name": "ysoseri-kb",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "tsc --noEmit",
    "sync:sample": "node crawler/sync.mjs --max-systems 3 --max-entries 20",
    "sync:ti": "node crawler/sync.mjs --systems Ti --max-entries 200",
    "test": "node --test crawler/*.test.mjs"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260828.1",
    "cheerio": "^1.1.2",
    "typescript": "^5.9.2",
    "wrangler": "^4.114.0"
  }
}
